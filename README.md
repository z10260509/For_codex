# For_codex

This repository contains a demo product page for the Google Pixel 9 smartphone.

## Usage
Open `index.html` in a browser to view the sample Pixel 9 page.
You can also run a simple local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000` in your browser.

